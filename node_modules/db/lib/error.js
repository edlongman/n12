exports.init=function(errors){
    this.data=errors;
};
exports.logError=function(code){
    var obj=this.data[code],
        x,
        mess="Error: ";
    mess+=code;
    for(x in obj){
        mess+='\n    '+x.charAt(0).toUpperCase() + x.slice(1)+': ' + obj[x];
    }
    console.log(mess);
};