var exports=function(options){
    if(!options.table){return 'No table selected'};
    if(typeof(options.table)!='string'){return 'Table should be a string'};
    var default_values=this.table[options.table];
    
}